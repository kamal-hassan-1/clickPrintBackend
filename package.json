{
  "name": "clickprintbackend",
  "scripts": {
    "dev": "nodemon --exec \"docker compose build && docker compose up -d\""
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "expo-server-sdk": "^6.1.0",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.4",
    "morgan": "^1.10.1",
    "multer": "^2.0.2"
  }
}
