info:
  name: Upload File
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/files"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/ahad/Downloads/ntn wecode.pdf
        contentType: application/pdf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
