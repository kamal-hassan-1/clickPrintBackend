info:
  name: Download File
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/files/:fileId"
  params:
    - name: fileId
      value: ""
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
