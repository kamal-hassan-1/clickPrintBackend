info:
  name: Delete User
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/users/:userId"
  params:
    - name: userId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "isDisabled": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
