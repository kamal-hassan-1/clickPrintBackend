info:
  name: Enable/Disable User
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/api/users/:userId"
  params:
    - name: userId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "isDisabled": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
