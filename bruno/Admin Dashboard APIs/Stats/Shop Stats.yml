info:
  name: Shop Stats
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/stats/shops"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
