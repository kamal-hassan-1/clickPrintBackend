info:
  name: Update Profile
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host}}/api/profile"
  body:
    type: json
    data: |-
      {
        "name": "Abdul Ahad"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
