info:
  name: Listen For Job Status Change
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
