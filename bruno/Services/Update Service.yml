info:
  name: Update Service
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/services/:serviceId"
  params:
    - name: serviceId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
