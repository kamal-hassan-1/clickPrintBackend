info:
  name: List Services
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/services"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
