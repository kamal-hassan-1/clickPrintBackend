info:
  name: Get All My Jobs
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/jobs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
