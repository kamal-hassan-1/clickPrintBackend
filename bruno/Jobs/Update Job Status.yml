info:
  name: Update Job Status
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}/api/jobs/:jobId/status"
  params:
    - name: jobId
      value: 6a40120b4f48dab8a0ee0ddb
      type: path
  body:
    type: json
    data: |-
      {
        "status": "completed"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
