info:
  name: Update Job Status
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}/api/jobs/:jobId/status"
  params:
    - name: jobId
      value: 6a4a0bef8211313da9b63db6
      type: path
  body:
    type: json
    data: |-
      {
        "status": "cancelled"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
