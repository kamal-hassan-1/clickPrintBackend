info:
  name: Update Draft By ID
  type: http
  seq: 6

http:
  method: PUT
  url: "{{host}}/api/drafts/:draftId"
  params:
    - name: draftId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
