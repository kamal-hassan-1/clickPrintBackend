info:
  name: Update Draft By ID
  type: http
  seq: 5

http:
  method: PUT
  url: "{{host}}/api/drafts/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
