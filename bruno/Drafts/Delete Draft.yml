info:
  name: Delete Draft
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/drafts/:draftId"
  params:
    - name: draftId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
