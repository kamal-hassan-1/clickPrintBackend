info:
  name: Create Draft
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/drafts"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
