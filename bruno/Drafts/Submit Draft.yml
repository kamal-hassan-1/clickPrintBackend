info:
  name: Submit Draft
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/api/drafts/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
