info:
  name: Replace Draft
  type: http
  seq: 2

http:
  method: PUT
  url: "{{host}}/api/drafts/:draftId"
  params:
    - name: draftId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
