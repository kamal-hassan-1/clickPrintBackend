info:
  name: Submit Draft As Job
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/api/drafts/:id/submit"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
