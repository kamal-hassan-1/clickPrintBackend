info:
  name: Create Printer
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/printers"
  body:
    type: json
    data: |-
      {
        "name": "HP LaserJet"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
