info:
  name: Delete Printer
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/printers/:printerId"
  params:
    - name: printerId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
