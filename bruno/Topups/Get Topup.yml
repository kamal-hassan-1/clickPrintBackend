info:
  name: Get Topup
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/topups/:topupId"
  params:
    - name: topupId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
