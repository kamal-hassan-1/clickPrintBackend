info:
  name: Raast Topup
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/topups/raast"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
