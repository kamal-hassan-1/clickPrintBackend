info:
  name: Approve/Decline
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/api/topups/:topupId"
  params:
    - name: topupId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "action": "approve"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
