info:
  name: Safepay Raast Event
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/webhooks/safepay"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
