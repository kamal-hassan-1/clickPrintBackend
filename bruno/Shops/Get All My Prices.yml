info:
  name: Get All My Prices
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/shops/:id/prices"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
