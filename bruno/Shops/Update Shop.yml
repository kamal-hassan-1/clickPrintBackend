info:
  name: Update Shop
  type: http
  seq: 3

http:
  method: PUT
  url: "{{host}}/api/shops/:shopId"
  params:
    - name: shopId
      value: 6a3cded8705669a8f5981125
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Ahad Prints",
        "address": "Westridge, Rawalpindi",
        "capabilities": ["bw", "a4", "color"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
