info:
  name: Create Topup
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/topups"
  body:
    type: json
    data: |-
      {
        "amount": 1000,
        "ppfid": "6a51c3dca6e1966cba786a08"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
