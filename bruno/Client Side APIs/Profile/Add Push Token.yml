info:
  name: Add Push Token
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/profile/pushTokens"
  body:
    type: json
    data: |-
      {
        "expoPushToken": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
