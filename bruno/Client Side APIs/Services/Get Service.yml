info:
  name: Get Service
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/services/:serviceId"
  params:
    - name: serviceId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
