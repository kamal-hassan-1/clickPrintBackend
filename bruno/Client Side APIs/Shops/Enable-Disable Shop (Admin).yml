info:
  name: Enable/Disable Shop (Admin)
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{host}}/api/shops/:shopId/isDisabled"
  params:
    - name: shopId
      value: 6a59bb07132d651de75c66e5
      type: path
  body:
    type: json
    data: |-
      {
        "isDisabled": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
