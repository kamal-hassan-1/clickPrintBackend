info:
  name: List Users (Admin)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
